SIM ?= questa
TEST ?= spi_base_test

.PHONY: compile run clean

compile:
	@echo "Add $(SIM) compile command using filelist.f"

run: compile
	@echo "Run $(TEST)"

clean:
	@echo "Remove simulator-generated files for $(SIM)"
